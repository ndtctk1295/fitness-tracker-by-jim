'use client';

/**
 * This is a forwarding file that maintains shadcn/ui v3 conventions
 * while preserving backward compatibility with the existing project structure.
 */

// Re-export everything from the existing implementation
export * from '@/lib/hooks/use-toast';
